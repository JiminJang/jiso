<html>
    <head>
        <title>인증 게시판</title>
    </head>
  
    <body>
        <div class="container col-9">
            <!-- index content -->
            <div class="index-content">
                <h3>인증 게시판</h3>
                <h5 style="text-align:left">본인이 알바생임을 인증하는 사진과 함께 올려주세요.</h5>
                <br>
                <table style="width:100%; border-top: 1px solid #D8D8D8; border-bottom: 1px solid #D8D8D8;">
                    <tr style="border-bottom: 1px solid #D8D8D8;">
                        <th style="width:10%;">
                            no.
                        </th>
                        <th style="width:50%;">
                            title
                        </th>
                        <th style="width:13%;">
                            writer
                        </th>
                        <th style="width:20%;">
                            date
                        </th>
                        <th style="width:7%;">
                            인증
                        </th>
                    </tr>
                    <% @brands.each do |p| %>
                        <tr>
                            <td class="no" style="text-align: center;">
                                <%=p.id%>
                            </td>
                            <td>
                                <a href="/brands/show/<%=p.id%>"><%= p.title %></a>
                            </td>
                            <td style="text-align: center;">
                                <%=p.user.name%>
                            </td>
                            <td class="date" style="text-align: center;">
                                <%=p.updated_at%>
                            </td>
                            <td class="like-count" style="text-align: center;">
    <%if p.user.has_role? 'alba' %>
        인증

    <% else p.user.has_role? 'user'%>
        미인증
        <%end%>

                                
                            </td>
                        </tr>
                    <% end %>
                </table>
                <br>
                <!--글쓰기 버튼-->
                <form action="/brands/new" style="float:left;">
                    <button type="submit" class="btn btn-secondary btn-sm">write</button>
                    
                </form>
            </div>
        </div>
    </body>
</html>
