<html>
    <head>
        <title>인증 게시판</title>
    </head>
  
    <body>
        <div class="container col-9">
            <div class= "show-content" style="border-bottom:solid 1px #5D5D5D">
                <h3>인증 게시판</h3>
                <p class="show-top1">subject &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<%= @brands.title %></p>
                <p class="show-top2">writer &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<%=@brands.user.name%></p>

                <p><%= @brands.content %></p>
                <img src="<%=@brands.image.url %>" alt="">
                <% if current_user.has_role? 'admin'%>
                    <%if @brands.user.has_role? 'alba' %>
                        <p>알바생 인증 되었습니다.</p>
                    <% else @brands.user.has_role? 'user'%>
                        <a href="/users/change/<%=@brands.id%>">등업하기</a>
                    <%end%>
   
                <%end%>
            <br>
            <br>
            </div>
            <br>
            <% if user_signed_in? %>
                 <% if current_user.has_role?'admin'%>  
                    <form action="/brands/destroy/<%=@brands.id%>" method="GET">
                        <button type="submit" class="btn btn-secondary btn-sm"  style="float:right;"formmethod="POST" formaction="/brands/destroy/<%=@brands.id%>" data-confirm="글을 삭제합니다">delete</button>
                                    
                    </form>
                 <%end%>
             <%end%>
         
             
        </div>
    </body>
</html>